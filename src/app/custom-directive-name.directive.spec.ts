import { CustomDirectiveNameDirective } from './custom-directive-name.directive';

describe('CustomDirectiveNameDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomDirectiveNameDirective();
    expect(directive).toBeTruthy();
  });
});
